# ИНСТРУКЦИЯ ДЛЯ КЛОДА (SYSTEM RULES)

Каждый раз, когда ты работаешь в этой папке, ты обязан строго соблюдать Манифест Проекта:
1. ЧИТАЙ ПРАВИЛА: Перед выполнением любой задачи прочитай файл чертежа сайта из Папки правил: `~/AI_Projects/danang-design-site_manifest.md`.
2. ЖЕСТКИЙ СТАНДАРТ ДЕСКТОПА: Глобальный контейнер всего сайта (`.site-wrapper` или `.w`) ОБЯЗАН быть зажат в `max-width: 1440px; margin: 0 auto; box-shadow: 0 0 50px rgba(0,0,0,0.15);` с серым фоном вокруг (`#f5f5f5`). Никакой бесконечной резины на компьютерах!
3. HERO-ЗАГОЛОВКИ: Текст "Добро пожаловать в Дананг" на десктопе обязан растягиваться в ОДНУ-ДВЕ широкие строки (убирай любые `max-width` меньше 1000px для этого текста). Размер адаптивный через `clamp()`. На мобильных — переход в 2 строки.
4. ГЕОМЕТРИЯ 2026: Полный запрет на овальные кнопки-"сосиски" и круглые инпуты. Форма всех элементов — только "мягкий квадрат" (`border-radius: 8px;`). Полностью удаляй все круглые плашки (badge) с карточек экскурсий.
5. БУДЬ КРАТОК: Не пиши вводную воду, не используй эмодзи и смайлики. Сразу применяй изменения в код.
